[package]
name = "gpu-guard-macros"
version = "0.1.0"
edition = "2021"

[package.metadata.release]
release = false

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
