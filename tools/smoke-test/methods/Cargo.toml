[package]
name = "methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "1.2.0-rc.1"
# Use this only for testing locally before a release.
# risc0-build = { path = "../../../risc0/build" }

[package.metadata.risc0]
methods = ["guest"]
