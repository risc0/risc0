diff --git a/conda.bzl b/conda.bzl
index 423877a..e98fb58 100644
--- a/conda.bzl
+++ b/conda.bzl
@@ -42,11 +42,6 @@ MINIFORGE_INSTALLER_NAME_TEMPLATE = "{minor}/Miniforge{major}-{minor}-{os}-{arch
 MINIFORGE_BASE_URL = "https://github.com/conda-forge/miniforge/releases/download/"
 
 INSTALLER_DIR = "installer"
-INSTALLER_FLAGS = {
-    "Windows": ["/InstallationType=JustMe", "/AddToPath=0", "/RegisterPython=0", "/S", "/D={}"],
-    "MacOSX": ["-b", "-f", "-p", "{}"],
-    "Linux": ["-b", "-f", "-p", "{}"],
-}
 
 CONDA_BUILD_FILE_TEMPLATE = """# This file was automatically generated by rules_conda
 
@@ -55,13 +50,13 @@ exports_files(['{conda}'])
 
 def _get_installer_flags(rctx, dir):
     os = get_os(rctx)
-    flags = INSTALLER_FLAGS[os]
 
     # insert directory
     dir = rctx.path(dir)
     if os == "Windows":
         dir = windowsify(dir)
-    return flags[:-1] + [flags[-1].format(dir)]
+        return ["/InstallationType=JustMe", "/AddToPath=0", "/RegisterPython=0", "/S", "/D={}".format(dir)]
+    return ["-p", dir, "-b", "-f"]
 
 # download conda installer
 def _download_conda(rctx):
