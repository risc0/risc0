[package]
name = "bn254-example"
version = "0.1.0"
edition = "2021"

default-run = "bn254-example"  # TODO?

[dependencies]
substrate-bn = "0.6.0"
bn254-methods = { path = "methods" }
risc0-zkvm = { path = "../../risc0/zkvm" }

# [features]
# TODO
# cuda = ["risc0-zkvm/cuda"]
# default = []
# prove = ["risc0-zkvm/prove"]
