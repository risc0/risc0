[package]
name = "revm-core"
version = "0.1.0"
edition = "2021"

[dependencies]
revm-methods = { path = "../methods" }
risc0-zkvm = { path = "../../../risc0/zkvm" }
serde = { version = "1.0", default-features = false }
hex = "0.4"