[package]
name = "bevy-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
bevy-core = { path = "../../core" }
bevy_ecs = "0.9.1"
risc0-zkvm = { path = "../../../../risc0/zkvm", default-features = false, features = [
  "std",
] }
