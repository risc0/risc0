[package]
name = "image_crop"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
image = { version = "0.24.5", default-features = false }
risc0-zkvm = { path = "../../../../risc0/zkvm", default-features = false, features = [
  "std",
] }
waldo-core = { path = "../../core", default-features = false }
