load("//bazel/rules/risc0:defs.bzl", "risc0_rust_proof")
load("@crates//:defs.bzl", "crates_from")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

risc0_rust_proof(
    name = "sign",
    srcs = ["src/bin/sign.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/device:r0vm-device",
        ":digital_signature_proof",
    ],
)

rust_library(
    name = "digital_signature_proof",
    srcs = ["src/lib.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/core:r0vm-core",
    ],
)

rust_test(
    name = "test",
    crate = ":digital_signature_proof",
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
)
