load("//bazel/rules/risc0:defs.bzl", "risc0_rust_proof")
load("@crates//:defs.bzl", "crates_from")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

risc0_rust_proof(
    name = "init_polling_station",
    srcs = ["src/bin/init_polling_station.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/device:r0vm-device",
        ":votingmachine_proof",
    ],
)

risc0_rust_proof(
    name = "submit_ballot",
    srcs = ["src/bin/submit_ballot.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/device:r0vm-device",
        ":votingmachine_proof",
    ],
)

risc0_rust_proof(
    name = "close_polling_station",
    srcs = ["src/bin/close_polling_station.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/device:r0vm-device",
        ":votingmachine_proof",
    ],
)

rust_library(
    name = "votingmachine_proof",
    srcs = ["src/lib.rs"],
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
    visibility = ["//visibility:public"],
    deps = crates_from(":Cargo.toml") + [
        "//risc0/r0vm/rust/core:r0vm-core",
    ],
)

rust_test(
    name = "test",
    crate = ":votingmachine_proof",
    rustc_flags = [
        "-Coverflow-checks=on",
    ],
)
