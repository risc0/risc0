[package]
name = "search_json"
version = "0.12.0"
edition = "2024"

[workspace]

[dependencies]
json = "0.12"
json-core = { path = "../../core" }
risc0-zkvm = { path = "../../../../risc0/zkvm", default-features = false, features = [
  "std",
] }
