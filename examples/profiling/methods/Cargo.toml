[package]
name = "fibonacci-methods"
version = "0.1.0"
edition = "2021"

[package.metadata.release]
release = false

[build-dependencies]
util = { path = "../../util" }

[package.metadata.risc0]
methods = ["guest"]

[features]
prove = ["util/prove"]
