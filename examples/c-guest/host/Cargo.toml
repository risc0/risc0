[package]
name = "c-guest-host"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
risc0-zkvm = { path = "../../../risc0/zkvm" }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[build-dependencies]
risc0-build = { path = "../../../risc0/build/" }
rzup = { path = "../../../rzup", default-features = false }
