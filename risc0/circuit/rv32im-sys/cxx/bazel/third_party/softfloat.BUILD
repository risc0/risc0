package(default_visibility = ["//visibility:public"])

# Export helper.c so it can be referenced from the main workspace
# The actual softfloat library is built in //rv32im/softfloat
exports_files([
    "helper.c",
])
