[package]
name = "risc0-fault-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { path = "../../zkvm", default-features = false, features = [ "std" ] }
rrs-lib = "0.1"
