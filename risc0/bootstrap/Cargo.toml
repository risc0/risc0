[package]
name = "bootstrap"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
risc0-zkp = { workspace = true }
risc0-zkvm = { workspace = true, features = ["default"] }

[package.metadata.release]
release = false
