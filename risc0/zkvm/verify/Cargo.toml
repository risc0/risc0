[package]
name = "risc0-zkvm-verify"
version = "0.1.0"
edition = "2021"
description = "RISC Zero zero-knowledge VM verify crate"
license = "Apache-2.0"
homepage = "https://risczero.com/"
repository = "https://github.com/risc0/risc0/"

[dependencies]
arrayref = "0.3"
lazy_static = "1.4"
risc0-zkp-core = { version = "0.1", path = "../../zkp/core"}
risc0-zkp-verify = { version = "0.1", path = "../../zkp/verify"}
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
env_logger = "0.9"
log = "0.4"
test-log = "0.2"
