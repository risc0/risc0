struct IoHandler {}

struct MemoryHandler {
    //   IoHandler* io;
}

struct MemoryState {
    // std::map<uint32_t, uint32_t> data;
    // std::set<MemoryEvent> history;
}

struct MemoryEvent {
    // uint32_t addr;
    // uint32_t cycle;
    // bool isWrite;
    // uint32_t data;
}
