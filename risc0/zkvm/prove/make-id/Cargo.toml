[package]
name = "risc0-make-id"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.2", features = ["derive"] }
risc0-zkvm = { version = "0.10", path = "../../sdk/rust" }

[package.metadata.release]
release = false
