if(ENABLE_TESTS)
	FILE( GLOB local_tests *.cpp )
	FOREACH( testsource ${local_tests} )
		GET_FILENAME_COMPONENT( filename ${testsource} NAME_WE )
		Add_risc0_test( "zkvm-prove" ${filename} "zkvm-prove core zkp-core zkp-accel-backend-cpu zkp-accel-kernels" )
	ENDFOREACH()
endif()
