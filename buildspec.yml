version: 0.2

phases:
  install:
    commands:
      curl -o bazelisk.exe https://github.com/bazelbuild/bazelisk/releases/download/v1.11.0/bazelisk-windows-amd64.exe
  build:
    commands:
      - bazelisk info
      - bazelisk test //...

cache:
  paths:
    - '$HOME/_bazel_*/**/*'
