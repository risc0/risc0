[profile.default]
src = "src"
out = "out"
libs = ["lib"]
ffi = true

# Allow solc to access files in the git project root.
# This option should be removed when this library is moved out of this repository.
allow_paths = ["../../lib"]

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
